apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: azure-tm-pacman
  namespace: pacman-app
  labels:
    app.kubernetes.io/name: pacman
spec:
  # Added for Azure TM demonstration
  host: pacman.digitaldovey.com
  path: "/"
  to:
    kind: Service
    name: pacman
